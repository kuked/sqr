#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "sqr"

def prompt
  print "db > "
end

loop do
  prompt
  input = gets.chomp

  if input == '.exit'
    exit(0)
  else
    puts "Unrecognized command '#{input}'."
  end
end
